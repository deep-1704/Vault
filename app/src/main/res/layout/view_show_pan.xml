<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Name on Card Item -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_credential_item"
        android:padding="12dp"
        android:layout_marginBottom="12dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_name_on_card"
            android:textAppearance="@style/TextAppearance.Vault.LabelCaps"
            android:textColor="@color/vault_on_surface_variant" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvNameOnCard"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="@style/TextAppearance.Vault.BodyLarge"
                android:textColor="@color/vault_on_surface"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btnCopyNameOnCard"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/action_copy"
                android:src="@drawable/ic_content_copy"
                app:tint="@color/vault_on_surface_variant" />
        </LinearLayout>
    </LinearLayout>

    <!-- PAN Number Item -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/bg_credential_item"
        android:padding="12dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_pan_number"
            android:textAppearance="@style/TextAppearance.Vault.LabelCaps"
            android:textColor="@color/vault_on_surface_variant" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tvPanNumber"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textAppearance="@style/TextAppearance.Vault.MonoMedium"
                android:textColor="@color/vault_on_surface"
                android:letterSpacing="0.08" />

            <ImageButton
                android:id="@+id/btnTogglePanNumber"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/hint_pan_number"
                android:src="@drawable/ic_visibility"
                app:tint="@color/vault_on_surface_variant" />

            <ImageButton
                android:id="@+id/btnCopyPanNumber"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginStart="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/action_copy"
                android:src="@drawable/ic_content_copy"
                app:tint="@color/vault_on_surface_variant" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
